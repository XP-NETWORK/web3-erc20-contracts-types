{
  "name": "web3-erc20-contracts-types",
  "version": "0.1.0",
  "main": "dist/index.js",
  "license": "MIT",
  "scripts": {
    "copy-dts": "copyfiles -u 1 \"src/**/*.d.ts\" dist",
    "build": "tsc -p tsconfig.json && yarn copy-dts"
  },
  "dependencies": {
    "ethers": "^5.5.2"
  },
  "devDependencies": {
    "typescript": "^4",
    "copyfiles": "^2"
  }
}
